CREATE TABLE zip_codes (
  postal_code       INT,
  place_name        VARCHAR(180),
  state_name        VARCHAR(100),
  province_name     VARCHAR(100),
  latitude          REAL,
  longitude         REAL
);
